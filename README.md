# Blockchain Production Log (Simulated)

Simple simulated blockchain project to demonstrate immutability vs SQLite baseline.

## Run locally

```bash
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python app.py
```

Open http://127.0.0.1:5000
